import 'package:flutter/material.dart';

class FrequentlySongsScreen extends StatelessWidget {
  const FrequentlySongsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: SafeArea(child: Center(child: Text("frequently played songs screen"),)),);
  }
}